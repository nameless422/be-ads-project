# Tasks: React BI Refactor

- [x] Create React + Vite frontend under `fe/`.
- [x] Move dashboard pages into route-level React pages.
- [x] Add shared API client and response types.
- [x] Serve `fe/dist` from the Go backend under `/bi`.
- [x] Update backend Makefile frontend commands to use `../fe`.
- [x] Keep root directory limited to `be/` and `fe/`.
- [x] Add OpenSpec and Spec Kit folders under `fe/`.
- [ ] Add generated API contract types if backend OpenAPI is introduced.
- [ ] Add browser smoke test script for `/bi/overview` and `/bi/control`.

